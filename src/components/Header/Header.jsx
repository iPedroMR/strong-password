import React from "react";
import "./Header.css";

const Header = () => (
  <header>
    <h1>Password Generator</h1>
    <p>
      Generate secure passwords that are strong enough to keep your online
      accounts safe.
    </p>
  </header>
);

export default Header;
